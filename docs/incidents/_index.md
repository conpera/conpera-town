# Incidents — immutable, dated, append-only records (decay: none)

A postmortem is decay-immune; the *lesson* it teaches lives in `docs/memory/` as a
`pit-*` that links back here. Never edit a closed incident; supersede with a new one.
